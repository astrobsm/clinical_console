# For OpenAI API integration
openai
Flask>=2.2
Flask-RESTful
Flask-SQLAlchemy
SQLAlchemy
Flask-Migrate
psycopg2-binary
Flask-JWT-Extended
Flask-Cors
Flask-Mail
python-dotenv

# For Swagger UI
flask-swagger-ui

# For password hashing and security (if not already present in your code, recommended for user auth)
Werkzeug

# For Alembic migrations (Flask-Migrate depends on this, but explicit is better)
alembic

# For JSON/YAML parsing (if you use YAML for OpenAPI specs)
PyYAML

# For background scheduling
apscheduler

# Production server
gunicorn
